package lk.AccessOne.print.event;

/** Nobody subscribes yet -- a candidate is a production-alerts dashboard once the reprint rate needs watching live. */
public record PrintQualityFailed(Long printJobId, Long cardId, String notes) { }
